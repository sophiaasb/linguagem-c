#include <stdio.h>
#include <locale.h>

int main() { 
	setlocale(LC_ALL,"");
   
   int valor, somaGeral = 0, contadorGeral = 0;
   int pares = 0, impares = 0;
   float mediaGeral, mediaPares;
   
   printf("Digite um número: ");
   scanf("%i",&valor);
   
   while (valor 1 - 0) {
   if (valor > 0) {
   	contadorGeral++;
   	somaGeral
   }
   }
}
